# Spitster

Spotify quiz game scaffold.

## Apps

- `apps/web`: React + Vite frontend
- `apps/api`: Node.js + Express backend
- `packages/shared`: shared types and validation

## Getting started

1. Copy `.env.example` to `.env` and fill in Spotify values.
2. Install dependencies.
3. Run the web and API apps together.
